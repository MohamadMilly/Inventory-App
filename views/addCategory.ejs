<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="/output.css" rel="stylesheet">
    <title><%= title %></title>
  </head>
  <body class="min-h-screen flex items-center flex-col space-y-4 bg-amber-50 py-10">
    <h1 class="text-2xl"><%= title %></h1>

    <form
      class="bg-amber-100 flex flex-col py-12 px-6 space-y-4 rounded max-w-md w-full sm:p-12"
      action="/categories/add"
      method="POST"
    >
      <label for="newCategory" class="text-gray-800">New category</label>
      <input
        class="p-1 rounded border border-amber-300 bg-amber-50 outline-blue-300"
        type="text"
        id="newCategory"
        name="newCategory"
        required
      >
       
      <% if (locals.errors && locals.errors.length > 0) { %>
        <div class="text-red-500 text-sm bg-amber-50 p-2 rounded">
          <ul class="list-inside flex flex-col">
            <% errors.forEach(error => { %>
              
                <li> - <%= error.msg %></li>
             
            <% }) %>
          </ul>
        </div>
      <% } %>

      <div class="flex space-x-1">
        <button
          class="my-4 bg-blue-400 text-white p-2 rounded cursor-pointer hover:bg-blue-500 transition-all duration-300 ease-in-out focus:outline-blue-500 outline-2 outline-offset-2 grow"
          type="submit"
        >
          Add
        </button>
        <a
          class="my-4 border-2 border-blue-400 text-blue-400 p-2 rounded cursor-pointer transition-all duration-300 ease-in-out focus:outline-blue-500 grow text-center hover:bg-blue-500 hover:text-white focus:outline-2"
          href="/"
        >
          Cancel
        </a>
      </div>
    </form>
  </body>
</html>
